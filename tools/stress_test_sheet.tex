% ============================================================
%  TeXLayout.jl — Stress Test Sheet  (reference LaTeX document)
%  Compile with:  pdflatex stress_test_sheet.tex
%
%  For New Computer Modern (closer to the :new_cm Julia default) use
%  XeLaTeX / LuaLaTeX and add:
%    \usepackage{unicode-math}
%    \setmathfont{New Computer Modern Math}
%
%  Package notes
%  ─────────────
%  wasysym [nointegrals]  – \yen  (before amssymb; nointegrals avoids
%                           \iint / \iiint clash with amsmath)
%  amssymb   – \varkappa, \beth, \gimel, \daleth, \angle,
%              \square, \lozenge, \bigstar, \checkmark, \maltese, …
%  mathtools – \overbracket, \underbracket
%  bm        – \boldsymbol
%  stmaryrd  – \bigsqcap
%  gensymb   – \degree
%  \overparen / \underparen are approximated via \overset / \underset
%  (for exact arc glyphs load yhmath, but that alters \widehat)
% ============================================================

\documentclass[10pt,a4paper,fleqn]{article}

% ── Layout ───────────────────────────────────────────────────────────────────
\usepackage[margin=5mm]{geometry}
\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ── Maths ────────────────────────────────────────────────────────────────────
\usepackage[nointegrals]{wasysym}   % \yen — load BEFORE amssymb
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}              % \overbracket, \underbracket
\usepackage{bm}                     % \boldsymbol
\usepackage{stmaryrd}               % \bigsqcap
\usepackage{gensymb}                % \degree

% Arc approximations (\overparen / \underparen).
% For proper extensible arcs load yhmath instead (alters \widehat).
\newcommand{\overparen}[1]{\overset{\frown}{#1}}
\newcommand{\underparen}[1]{\underset{\smile}{#1}}

% ── Misc ─────────────────────────────────────────────────────────────────────
\usepackage{xcolor}
\usepackage{microtype}

% ── Left-align display math with no indent ───────────────────────────────────
\setlength{\mathindent}{0pt}

% ── Tight display-math vertical spacing ──────────────────────────────────────
\setlength{\abovedisplayskip}{3pt plus 1pt minus 1pt}
\setlength{\belowdisplayskip}{3pt plus 1pt minus 1pt}
\setlength{\abovedisplayshortskip}{1pt}
\setlength{\belowdisplayshortskip}{1pt}

% ── Full-width dark title bar ─────────────────────────────────────────────────
\newcommand{\titlebar}[1]{%
  \noindent
  {\setlength{\fboxsep}{5pt}%
   \colorbox{black!90}{%
     \makebox[\dimexpr\linewidth-2\fboxsep][l]{%
       \color{white}\bfseries\large\strut\enspace #1%
     }}}%
  \par\nobreak\vspace{1pt}%
}

% ── Thin rule between sections ────────────────────────────────────────────────
\newcommand{\secdiv}{%
  \par\nobreak\vspace{1pt}%
  \noindent\rule{\linewidth}{0.5pt}%
  \par\nobreak\vspace{1pt}%
}

% ─────────────────────────────────────────────────────────────────────────────
\begin{document}

\titlebar{TeXLayout.jl\enspace STRESS TEST\enspace ---\enspace COMPUTER MODERN MATH}

% ── 1. DEEP NESTING — CONTINUED FRACTIONS ────────────────────────────────────
\[
  \frac{1}{1+\frac{1}{1+\frac{1}{1+\frac{1}{2}}}}
  \quad
  \frac{\frac{a+b}{c-d}}{\frac{e+f}{g-h}+\frac{i}{j+k}}
  \quad
  \frac{1}{\sqrt{1+\frac{x^2}{1+\frac{x^4}{1+x^6}}}}
\]
\secdiv

% ── 2. DEEP NESTING — RADICALS & SCRIPTS ─────────────────────────────────────
\[
  \sqrt{1+\sqrt{1+\sqrt{1+\sqrt{1+x}}}}
  \quad
  x^{a^{b^{c^d}}}+y_{m_{n_{p_q}}}
  \quad
  \left(\frac{p}{q}\right)^{\!\left(\frac{r}{s}\right)^{\!2}}
\]
\secdiv

% ── 3. CLASSIC IDENTITIES ────────────────────────────────────────────────────
\[
  e^{i\pi}+1=0
  \quad
  \sum_{n=1}^{\infty}\frac{1}{n^2}=\frac{\pi^2}{6}
  \quad
  \left(\sum_{k=1}^{n}k\right)^{\!2}=\sum_{k=1}^{n}k^3
  \quad
  \prod_{n=1}^{\infty}\!\left(1-\frac{x^2}{n^2\pi^2}\right)=\frac{\sin x}{x}
\]
\secdiv

% ── 4. INTEGRALS & GREEN'S THEOREM ───────────────────────────────────────────
\[
  \int_{-\infty}^{\infty}e^{-x^2/2}\,dx=\sqrt{2\pi}
  \quad
  \frac{d}{dx}\!\left(\int_a^x f(t)\,dt\right)=f(x)
  \quad
  \iint_D\!\left(\frac{\partial Q}{\partial x}-\frac{\partial P}{\partial y}\right)dx\,dy
    =\oint_{\partial D}P\,dx+Q\,dy
\]
\secdiv

% ── 5. LARGE OPERATORS — SIGMA / PI / INTEGRAL FAMILY ────────────────────────
\[
  \sum_{k=0}^{n}\frac{(-1)^k}{2k+1}
  \quad
  \prod_{p\,\text{prime}}\frac{p^s}{p^s-1}
  \quad
  \coprod_{\alpha\in I}X_\alpha
  \quad
  \int_0^1 f\,dx\;\;\iint_D f\,dx\,dy\;\;\iiint_V f\,dV
  \quad
  \oint_C\mathbf{F}\cdot d\mathbf{r}
\]
\secdiv

% ── 6. LARGE OPERATORS — SET / LATTICE FAMILY ────────────────────────────────
\[
  \bigcup_{n=1}^{\infty}A_n\;\bigcap_{n=1}^{\infty}B_n
  \quad
  \bigsqcup_{k\geq0}C_k\;\bigsqcap_{k\geq0}D_k
  \quad
  \bigvee_{i\in I}P_i\;\bigwedge_{i\in I}Q_i
  \quad
  \bigoplus_{k=1}^n V_k\;\bigotimes_{k=1}^n W_k\;\bigodot_k Z_k\;\biguplus_k U_k
\]
\secdiv

% ── 7. PHYSICS EQUATIONS ─────────────────────────────────────────────────────
\[
  \hat{H}\psi=-\frac{\hbar^2}{2m}\nabla^2\psi+V(\mathbf{r})\psi=E\psi
  \quad
  \nabla\times\mathbf{B}=\mu_0\mathbf{J}+\mu_0\varepsilon_0\frac{\partial\mathbf{E}}{\partial t}
  \quad
  \dot{\mathbf{q}}=\frac{\partial\mathcal{H}}{\partial\mathbf{p}},\;
  \dot{\mathbf{p}}=-\frac{\partial\mathcal{H}}{\partial\mathbf{q}}
\]
\secdiv

% ── 8. TALL AUTO-SIZED DELIMITERS ────────────────────────────────────────────
\[
  \left(\frac{\dfrac{a}{b}+\dfrac{c}{d}}{\dfrac{e}{f}}\right)^{\!3}
  \quad
  \left\|\frac{\partial^2 f}{\partial x^2}\right\|_2
    +\left\lfloor\frac{\lceil x\rceil}{2}\right\rfloor
  \quad
  \left\langle\frac{a}{b}\,\middle|\,\frac{c}{d}\right\rangle
  \quad
  \left[\begin{matrix}\frac{1}{2}&-\frac{1}{2}\\[2pt]\frac{1}{2}&\frac{1}{2}\end{matrix}\right]
\]
\secdiv

% ── 9. GREEK ALPHABET — LOWERCASE ────────────────────────────────────────────
\[
  \alpha\;\beta\;\gamma\;\delta\;\varepsilon\;\zeta\;\eta\;\theta\;
  \iota\;\kappa\;\lambda\;\mu\;\nu\;\xi\;\pi\;\varpi\;\rho\;\varrho\;
  \sigma\;\varsigma\;\tau\;\upsilon\;\varphi\;\chi\;\psi\;\omega
  \quad
  \epsilon\;\vartheta\;\varkappa\;\phi
\]
\secdiv

% ── 10. GREEK ALPHABET — UPPERCASE + MISC ────────────────────────────────────
\[
  \Gamma\;\Delta\;\Theta\;\Lambda\;\Xi\;\Pi\;\Sigma\;\Upsilon\;\Phi\;\Psi\;\Omega
  \quad
  \hbar\;\ell\;\partial\;\nabla\;\infty\;\forall\;\exists\;\emptyset\;
  \aleph\;\beth\;\gimel
  \quad
  \Re\;\Im\;\wp\;\imath\;\jmath
\]
\secdiv

% ── 11. FONT VARIANTS — LATIN & GREEK ────────────────────────────────────────
\[
  \mathbf{AaBbXx}\;\;\mathit{AaBbXx}
  \quad
  \mathrm{AaBbXx}\;\;\mathsf{AaBbXx}\;\;\mathtt{AaBbXx}
  \quad
  \mathbb{RCZQN}\;\;\mathcal{FLHKP}\;\;\mathfrak{fgAB}
  \quad
  \boldsymbol{\alpha\beta\gamma\Gamma\Delta\Omega}\;\;
  \mathbf{x}^{\mathbf{T}}\mathbf{A}\mathbf{x}
\]
\secdiv

% ── 12. ACCENTS — NON-EXTENSIBLE ─────────────────────────────────────────────
\[
  \hat{f}\;\bar{x}\;\vec{v}\;\dot{q}\;\ddot{y}
  \quad
  \tilde{a}\;\breve{u}\;\check{c}\;\acute{e}\;\grave{e}\;\mathring{A}
  \quad
  \hat{\mathbf{n}}+\vec{\mathbf{F}}\times\bar{\mathbf{B}}
\]
\secdiv

% ── 13. ACCENTS — EXTENSIBLE (widehat / widetilde) ───────────────────────────
\[
  \widehat{x}+\widehat{xy}+\widehat{xyz}+\widehat{xyzw}
  \quad
  \widetilde{a}+\widetilde{ab}+\widetilde{abc}+\widetilde{abcd}
  \quad
  \widehat{f\cdot g}=\hat{f}*\hat{g}
\]
\secdiv

% ── 14. HORIZONTAL BRACES ────────────────────────────────────────────────────
\[
  \overbrace{a_1+a_2+\cdots+a_{n-1}+a_n}^{n\text{ terms}}
  \quad
  \underbrace{f(x_1)\cdot f(x_2)\cdots f(x_n)}_{n\text{ factors}}\leq M^n
  \quad
  \overbracket{p_1+p_2+\cdots+p_k}^{\text{sum}}\;
  \underbracket{q_1\cdot q_2\cdots q_m}_{\text{product}}
  \quad
  \overparen{\alpha+\beta}+\underparen{\gamma+\delta}
\]
\secdiv

% ── 15. MATRICES — ENVIRONMENT GALLERY ───────────────────────────────────────
\[
  \begin{pmatrix}
    \frac{\partial^2 f}{\partial x^2}&\frac{\partial^2 f}{\partial x\partial y}\\[4pt]
    \frac{\partial^2 f}{\partial y\partial x}&\frac{\partial^2 f}{\partial y^2}
  \end{pmatrix}
  \quad
  \det\begin{pmatrix}
    1{-}\lambda&1&0\\0&1{-}\lambda&1\\0&0&1{-}\lambda
  \end{pmatrix}=(1-\lambda)^3
  \quad
  \begin{Bmatrix}a&b\\c&d\end{Bmatrix}
  \;\;\begin{Vmatrix}p&q\\r&s\end{Vmatrix}
\]
\secdiv

% ── 16. MATRICES — ARRAY COLSPEC & CASES ─────────────────────────────────────
\[
  \begin{array}{|r|c|l|}
    \alpha&\beta&\gamma\\\frac{1}{2}&\sqrt{3}&\pi^2
  \end{array}
  \quad
  \begin{cases}
    x^2&\text{if }x\ge0\\-x^2&\text{if }x<0
  \end{cases}
  \quad
  \begin{array}{||c||}
    \frac{a+b}{c}\\d
  \end{array}
\]
\secdiv

% ── 17. LIMITS OVERRIDE (\limits / \nolimits) ────────────────────────────────
\[
  \int\limits_0^{\infty}e^{-st}f(t)\,dt=\mathcal{L}\{f\}(s)
  \quad
  \sum\nolimits_{k=0}^{n}x^k=\frac{x^{n+1}-1}{x-1}
  \quad
  \int_{h\to0}\frac{f(x+h)-f(x)}{h}
  \;\text{ vs. }\;
  \int\limits_{h\to0}\frac{f(x+h)-f(x)}{h}
\]
\secdiv

% ── 18(a). NAMED OPERATORS ───────────────────────────────────────────────────
\[
  \sin^2\theta+\cos^2\theta=1,\;\tan\theta=\frac{\sin\theta}{\cos\theta}
  \quad
  \log(ab)=\log a+\log b,\;\ln e^x=x,\;\exp(i\pi)=-1
  \quad
  \lim_{x\to0^+}x\ln x=0,\;
  \limsup_{n\to\infty}a_n,\;
  \liminf_{n\to\infty}b_n
\]
\secdiv

% ── 18(b). NAMED OPERATORS ───────────────────────────────────────────────────
\[
  \sup_{x\in A}f(x),\;\inf_{x\in A}f(x),\;\max_k a_k,\;\min_k b_k
  \quad
  \det A=\sum_\sigma\text{sgn}(\sigma)\prod_i a_{i\sigma(i)},\;
  \ker T\cap\text{Im}\,S
  \quad
  \gcd(a,b)\cdot\operatorname{lcm}(a,b)=ab,\;\deg p=n,\;\dim V=n
\]
\secdiv

% ── 19. INTER-ATOM SPACING ───────────────────────────────────────────────────
\[
  a+b-c\times d\div e=f
  \quad
  A\cup B\cap C\setminus D\oplus E
  \quad
  x\leq y\geq z,\;p\Rightarrow q\iff r
  \quad
  \{a,b,c\}\subset\langle d,e\rangle\subseteq\mathbb{R}^n
\]
\secdiv

% ── 20. ITALIC CORRECTION ON SLANTED BASES ───────────────────────────────────
\[
  \int_a^b f(x)\,dx\ne\int_0^1 g(t)\,dt
  \quad
  \int\!\!\int_D f\,dA\quad\iint_D f\,dA
  \quad
  \oint_C f\,dz=2\pi i\sum_k\operatorname{Res}(f,z_k)
\]
\secdiv

% ── 21. SYMBOL GALLERY — ARROWS & LOGIC ──────────────────────────────────────
\[
  A\to B\leftarrow C,\;f\colon X\mapsto Y
  \quad
  \forall\varepsilon>0\;\exists\delta>0\colon
    |x-a|<\delta\implies|f(x)-L|<\varepsilon
  \quad
  P\Leftrightarrow Q,\;\neg P\Rightarrow R,\;A\vdash B
\]
\secdiv

% ── 22. SYMBOL GALLERY — MISC ORDINALS ───────────────────────────────────────
\[
  {\prime}\;\partial\;\nabla\;\angle\;\triangle\;\square\;\lozenge\;\bigstar\;
  \flat\;\natural\;\sharp\;\checkmark\;\maltese\;\degree\;\yen\;\pounds\;
  \top\;\bot\;\aleph\;\beth\;\gimel\;\daleth\;\hbar\;\ell
\]
\secdiv

% ── 23. KITCHEN SINK — FOURIER & LAPLACE ─────────────────────────────────────
\[
  \hat{f}(\xi)=\int_{-\infty}^{\infty}f(x)\,e^{-2\pi ix\xi}\,dx
  \quad
  \mathcal{L}\{f*g\}(s)=\mathcal{L}\{f\}(s)\cdot\mathcal{L}\{g\}(s)
  \quad
  \sum_{n=-\infty}^{\infty}c_n\,e^{in\theta}\xrightarrow{\;L^2\;}f(\theta)
\]
\secdiv

% ── 24. KITCHEN SINK — TAYLOR & POWER SERIES ─────────────────────────────────
\[
  \sum_{n=0}^{\infty}\frac{f^{(n)}(a)}{n!}(x-a)^n=f(x)
  \quad
  e^x=\sum_{n=0}^{\infty}\frac{x^n}{n!},\;
  \sin x=\sum_{n=0}^{\infty}\frac{(-1)^n x^{2n+1}}{(2n+1)!}
  \quad
  \frac{1}{1-x}=\sum_{n=0}^{\infty}x^n\;(|x|<1)
\]

\end{document}
