# Todo

## Current

- [x] Add a MathTeXEngine-style command-line visualisation tool in `tools/` for arbitrary expressions, then document and validate it.
- [x] Add a CairoMakie-backed `visualise_metrics_makie.jl` variant that renders the expression with `text!` and overlays TeXLayout-derived metric boxes.

## Future

- None currently recorded.
